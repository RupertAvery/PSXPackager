<UserControl x:Class="PSXPackagerGUI.Controls.ResourceButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:PSXPackagerGUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="120">
    <UserControl.Resources>
        <converters:NotConverter x:Key="NotConverter"></converters:NotConverter>
    </UserControl.Resources>
    <Button HorizontalContentAlignment="Left" Click="ButtonBase_OnClick"
            DataContext="{Binding ., RelativeSource={RelativeSource AncestorLevel=1, AncestorType=UserControl}}">
        <Button.Style>
            <Style TargetType="Button">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                        <Setter Property="Background" Value="{x:Static SystemColors.ControlDarkBrush}"></Setter>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
        <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
            <CheckBox IsEnabled="{Binding IsFixed, Converter={StaticResource NotConverter}}" IsChecked="{Binding IsChecked}" VerticalContentAlignment="Center"></CheckBox>
            <Label VerticalContentAlignment="Center" Content="{Binding Label}"></Label>
        </StackPanel>
    </Button>
</UserControl>
